services:
  haejoong-server-gateway:
    build: .
    container_name: haejoong-server-gateway
    restart: always
    environment:
      - TZ=Asia/Seoul
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/var/log/nginx
    networks:
      - gateway

networks:
  gateway:
    name: gateway
